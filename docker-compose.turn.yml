services:
  coturn:
    image: coturn/coturn:latest
    container_name: orion-coturn
    restart: unless-stopped
    network_mode: host
    command:
      - -c
      - /etc/coturn/turnserver.conf
    volumes:
      - ./turnserver.conf.example:/etc/coturn/turnserver.conf:ro
